# 🧠 加密货币趋势跟踪量化系统

一个基于海龟交易系统的加密货币趋势跟踪量化交易框架。

## 📋 项目特点

- **多维度趋势识别**: 整合ADX、Hurst指数、效率比率等多个指标
- **海龟交易系统**: 实现突破策略和均线交叉策略
- **动态仓位管理**: 基于ATR的风险控制和波动率目标
- **投资组合优化**: 支持风险平价和趋势权重分配
- **参数优化**: 贝叶斯优化、网格搜索、Walk-Forward分析
- **稳健性测试**: 参数扰动、蒙特卡洛模拟
- **可视化分析**: 权益曲线、回撤、热力图等
- **完整报告**: HTML格式的回测报告

## 🚀 快速开始

### 安装依赖

```bash
pip install pandas numpy matplotlib seaborn pyyaml scipy
```

### 准备数据

将历史K线数据放入 `data/` 目录，文件命名为 `btc.csv`, `eth.csv` 等。

CSV格式要求包含以下列：
- timestamp (日期时间)
- open (开盘价)
- high (最高价)
- low (最低价)
- close (收盘价)
- volume (成交量)

### 配置系统

编辑 `config/settings.yaml` 配置文件，设置：
- 资产池
- 趋势识别参数
- 信号生成参数
- 仓位管理参数
- 风险限制

### 运行系统

```bash
# 趋势分析
python main.py trend

# 单资产回测
python main.py single --symbol BTC

# 多资产组合回测
python main.py portfolio

# 参数优化
python main.py optimize
```

## 📁 项目结构

```
trend_system/
│
├── config/                     # 配置文件
│   ├── settings.yaml          # 全局配置
│   └── credentials.yaml       # API密钥
│
├── data/                       # 历史数据
│   ├── btc.csv
│   └── eth.csv
│
├── core/                       # 核心模块
│   ├── data_loader.py         # 数据加载
│   ├── indicators.py          # 技术指标
│   ├── trend_detector.py      # 趋势识别
│   ├── signal_generator.py    # 信号生成
│   ├── position_sizing.py     # 仓位管理
│   ├── portfolio_allocator.py # 组合分配
│   └── risk_manager.py        # 风险管理
│
├── optimization/               # 优化模块
│   ├── parameter_search.py    # 参数搜索
│   ├── robustness_tests.py    # 稳健性测试
│   └── asset_selection.py     # 资产筛选
│
├── backtest/                   # 回测模块
│   ├── backtester.py          # 回测引擎
│   ├── metrics.py             # 绩效指标
│   ├── visualization.py       # 可视化
│   └── performance_report.py  # 报告生成
│
├── research/                   # 研究笔记本
│   ├── trend_analysis.ipynb
│   ├── parameter_heatmap.ipynb
│   └── asset_compare.ipynb
│
├── prompt/                     # 提示词模板
│
├── main.py                     # 主入口
└── README.md
```

## 🧪 核心功能模块

### 1. 趋势识别

使用多个指标综合判断市场趋势：
- **ADX**: 判断趋势强度
- **Hurst指数**: 衡量时间序列的趋势性/均值回归特性
- **效率比率(ER)**: 衡量价格变动效率
- **动量**: 价格变化率

### 2. 信号生成

支持两种策略：
- **突破系统**: 唐奇安通道突破（海龟1号）
- **均线交叉**: 快慢均线交叉

### 3. 仓位管理

- 基于ATR的动态仓位计算
- 波动率目标调整
- 跟踪止损
- 风险限制

### 4. 投资组合管理

- 风险平价分配
- 趋势权重分配
- 相关性过滤
- 定期再平衡

### 5. 参数优化

- 网格搜索
- 随机搜索
- 贝叶斯优化
- Walk-Forward分析
- 参数敏感性测试
- 蒙特卡洛模拟

## 📊 绩效指标

系统计算以下关键指标：

- **收益指标**: 总收益率、年化收益率
- **风险指标**: 波动率、最大回撤、VaR、CVaR
- **风险调整收益**: 夏普比率、索提诺比率、卡玛比率
- **交易指标**: 胜率、盈利因子、交易次数

## 🔬 研究流程

建议按以下顺序执行研究：

| 周次 | 任务 | 输出 |
|------|------|------|
| 第1周 | 数据与趋势检测模块 | 各币趋势得分表 |
| 第2周 | 信号与仓位模块 | 单币回测图表 |
| 第3周 | 参数优化模块 | entry/exit热力图 |
| 第4周 | 稳健性测试模块 | 扰动与WFA报告 |
| 第5周 | 多币组合与最终报告 | 组合绩效与监控 |

## 📈 使用示例

### 示例1: 趋势分析

```python
from core.data_loader import DataLoader
from core.trend_detector import TrendDetector

loader = DataLoader()
data_dict = loader.load_all_assets()

detector = TrendDetector()
trend_scores = detector.get_trending_assets(data_dict)
print(trend_scores)
```

### 示例2: 单资产回测

```python
from core.data_loader import DataLoader
from core.signal_generator import SignalGenerator
from backtest.backtester import Backtester

# 加载数据
loader = DataLoader()
btc_data = loader.load_single_asset('BTC')

# 生成信号
signal_gen = SignalGenerator()
signals = signal_gen.generate_signals(btc_data)

# 回测
backtester = Backtester()
equity_curve = backtester.run_backtest({'BTC': btc_data}, {'BTC': signals})
```

## ⚙️ 配置说明

主要配置项（`config/settings.yaml`）：

```yaml
# 资产池
assets:
  symbols: [BTC, ETH, BNB, SOL, ADA]

# 趋势识别
trend_detection:
  adx_threshold: 25
  hurst_threshold: 0.5
  trend_score_threshold: 0.6

# 信号生成
signal:
  entry_period: 20
  exit_period: 10
  signal_type: breakout  # 或 ma_cross

# 仓位管理
position_sizing:
  risk_per_trade: 0.01
  atr_multiplier: 2.0
  max_position: 0.3

# 风险管理
risk_management:
  max_drawdown: 0.20
  max_exposure: 1.0
```

## 📝 注意事项

1. **数据质量**: 确保历史数据完整且准确
2. **参数设置**: 根据不同市场环境调整参数
3. **风险控制**: 严格遵守风险限制
4. **回测偏差**: 注意过拟合和前视偏差
5. **实盘测试**: 先进行纸上交易验证

## 🤝 贡献

欢迎提交问题和拉取请求！

## 📄 许可证

MIT License

## 📧 联系方式

如有问题，请提交 Issue。

---

**⚠️ 风险提示**: 本系统仅供学习和研究使用。加密货币交易存在高风险，可能导致本金损失。请谨慎使用，并根据自身情况评估风险。
